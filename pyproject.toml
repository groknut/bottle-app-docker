[project]
name = "bottle-app-docker"
version = "0.1.0"
description = "Практикум Docker - создание веб-приложения в контейнере"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bottle>=0.13.4",
]
